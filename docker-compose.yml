services:
  backend:
    build:
      context: .
      dockerfile: infra/docker/backend/Dockerfile
    ports:
      - "8000:8000"

  frontend:
    build:
      context: .
      dockerfile: infra/docker/frontend/Dockerfile
    depends_on:
      - backend
    ports:
      - "4200:4200"
